import { ProgressWidthDirective } from './progress-width.directive';

describe('ProgressWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressWidthDirective();
    expect(directive).toBeTruthy();
  });
});
